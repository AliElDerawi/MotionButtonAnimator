<resources>

    <!-- Base application theme. -->
    <style name="AppTheme" parent="Theme.MaterialComponents.Light.NoActionBar">
        <!-- Customize your theme here. -->
        <item name="colorPrimary">@color/colorPrimary</item>
        <item name="colorPrimaryVariant">@color/colorPrimaryDark</item>
        <item name="colorOnPrimary">@color/colorDark1C</item>
        <item name="android:statusBarColor">@color/colorPrimary</item>

        <!-- Secondary brand color. -->
        <item name="colorSecondary">@color/colorAccent</item>
        <item name="colorSecondaryVariant">@color/colorAccent</item>
        <item name="colorOnSecondary">@color/colorWhite</item>
        <item name="colorAccent">@color/colorAccent</item>

        <item name="android:windowDisablePreview">true</item>
        <item name="android:windowBackground">@color/colorWhite</item>

    </style>

    <style name="AppTheme.NoActionBar">
        <item name="windowActionBar">false</item>
        <item name="windowNoTitle">true</item>
    </style>

    <style name="AppTheme.AppBarOverlay" parent="ThemeOverlay.MaterialComponents.Dark.ActionBar" />
    <style name="AppTheme.PopupOverlay" parent="ThemeOverlay.MaterialComponents.Light" />

    <style name="RadioButtonStyle">
        <item name="android:textSize">@dimen/dim_sub_title_text_size</item>
        <item name="android:textAlignment">center</item>
        <item name="android:buttonTint">@drawable/radiobutton_color_filter_tint</item>
        <item name="android:textColor">@drawable/radiobutton_text_color_filter_tint</item>
        <item name="android:padding">@dimen/dim_4dp</item>
    </style>

    <style name="DetailTextStyle">
        <item name="android:textSize">@dimen/dim_sub_title_text_size</item>
        <item name="android:textColor">@color/colorGray9a</item>
        <item name="android:textStyle">bold</item>
    </style>

    <style name="ButtonStyle" parent="ThemeOverlay.Material3.Button">
        <item name="android:textSize">@dimen/dim_button_text_size</item>
        <item name="android:textColor">@color/colorWhite</item>
        <item name="android:textStyle">bold</item>
        <item name="android:insetTop">0dp</item>
        <item name="android:insetBottom">0dp</item>
        <item name="cornerRadius">@dimen/dim_margin_between_views</item>
    </style>

    <style name="ToolbarStyle" parent="Widget.Material3.Toolbar">
        <item name="android:background">@color/colorPrimary</item>
        <item name="titleTextAppearance">@style/CustomToolbarTitleText</item>
    </style>

    <style name="CustomToolbarTitleText" parent="TextAppearance.Widget.AppCompat.Toolbar.Title">
        <item name="android:textSize">@dimen/dim_toolbar_text_size</item>
    </style>

</resources>
